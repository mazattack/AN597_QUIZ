---
title: "Quiz01"
author: "Maria C. Codlin"
date: "September 26, 2017"
output: html_document
---
Above 5.5 and including, and below 5.5
number of zombies kiled
```{r}
library(curl)
f <- f <- curl("https://raw.githubusercontent.com/fuzzyatelin/fuzzyatelin.github.io/master/AN597_Fall17/zombies.csv")
d <- read.csv(f, header = TRUE, sep = ",", stringsAsFactors = FALSE)
head(d)
```
```{r}
library(dplyr)
d<- d%>%filter (gender=="Female")%>%
  select(first_name:last_name)%>%
  print
```

```{r}
library(dplyr)
d<- d%>%filter (gender=="Female")%>%
  select(first_name:last_name)%>%
  print
```
